# Minimal set to run the API in "dev/demo mode": in-memory graph store,
# embedded Qdrant, stub NLP/OCR/biometrics. Good enough to boot the server,
# hit every endpoint, and develop the frontend against it.
#
#   pip install -r requirements-dev.txt
#
# Install requirements.txt instead (or alongside) once you wire up real
# Neo4j / Kafka / Indic-BERT / ArcFace.

fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1           # pinned: bcrypt>=4.1 dropped the `__about__` attr passlib 1.7.4 reads
cryptography==42.0.8
neo4j==5.22.0
networkx==3.3
scipy==1.13.1           # required by networkx.pagerank's scipy-backed solver
qdrant-client==1.9.1
kafka-python==2.0.2
httpx==0.27.0
python-dotenv==1.0.1
reportlab==4.2.0
