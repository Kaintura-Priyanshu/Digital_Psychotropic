# --- Core API ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
python-multipart==0.0.9

# --- Auth / security ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1           # pinned: bcrypt>=4.1 dropped the `__about__` attr passlib 1.7.4 reads
cryptography==42.0.8

# --- Graph store ---
neo4j==5.22.0          # official driver; app falls back to an in-memory
                        # NetworkX graph automatically if no NEO4J_URI is reachable
networkx==3.3
scipy==1.13.1           # required by networkx.pagerank's scipy-backed solver

# --- Vector store (biometric similarity) ---
qdrant-client==1.9.1    # runs in embedded/in-memory mode out of the box,
                        # point QDRANT_URL at a real server for production

# --- Messaging ---
kafka-python==2.0.2     # CDR stream producer; safe to import without a broker running

# --- Ingestion (optional / heavy — imported lazily, app runs without them) ---
opencv-python-headless==4.10.0.84
pytesseract==0.3.10     # requires the `tesseract-ocr` system package
easyocr==1.7.1          # requires torch; large download, CPU-only is fine
python-magic==0.4.27

# --- NLP (optional / heavy — imported lazily) ---
transformers==4.42.3
torch==2.3.1
sentence-transformers==3.0.1
metaphone==0.6
python-Levenshtein==0.25.1

# --- Biometrics (optional / heavy — imported lazily) ---
insightface==0.7.3      # ArcFace embeddings
onnxruntime==1.18.1

# --- Agents ---
langchain==0.2.6
langchain-anthropic==0.1.19

# --- PDF dossier export ---
reportlab==4.2.0

# --- Misc ---
httpx==0.27.0
python-dotenv==1.0.1
